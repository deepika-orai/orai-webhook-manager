# Frontend Environment Variables (Placeholders)
# Server-only proxy target for Next.js external rewrites (/backend-api/* -> ${API_PROXY_TARGET}/api/*)
# Local development default: http://localhost:5135
# Vercel Production configuration: https://oraiapi.azurewebsites.net
API_PROXY_TARGET=http://localhost:5135

# Demo / Development Tenant Context Header (Never commit real production IDs)
NEXT_PUBLIC_DEMO_TENANT_ID=00000000-0000-0000-0000-000000000000
